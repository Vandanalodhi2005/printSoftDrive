/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  images: {
    domains: ["images.unsplash.com", "placehold.co", "www.printsoftdriver.com", "printsoftdriver.com"],
  }
}

module.exports = nextConfig
