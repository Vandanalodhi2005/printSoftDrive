# PrintSoftDriver

Next.js 15 App Router TypeScript project scaffold for PrintSoftDriver.

Setup:

```bash
npm install
npm run dev
```

Environment:

Create a `.env.local` with:

```
EMAIL_HOST=
EMAIL_PORT=
EMAIL_USER=
EMAIL_PASS=
```
